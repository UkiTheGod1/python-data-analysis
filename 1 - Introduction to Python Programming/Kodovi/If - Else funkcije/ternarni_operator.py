# status = "član"
# if status == "član":
    # popust = 10
# else:
    # popust = 0
# print(f"Popust je {popust}%")

status = input("Unesi status: ")
popust = 10 if status == "clan" else 0
print(f"Popust je {popust}%")