user_info <- list(
  name = "Ana",
  books_borrowed = 5,
  is_member = TRUE
)

user_info # Prikazuje elemente liste

user_info$name # Prikazuje vrednost od "name"

user_info[["books_borrowed"]] # Alternativni nacin